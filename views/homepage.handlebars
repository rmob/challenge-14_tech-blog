<section class="container">
  <div class="page-title">Blog Posts</div>
  {{#each entries as |entry|}}
    <a href="/entry/{{id}}">
      <div class="page-title">{{title}}</div>
      <div class="byline">By: {{user.user_name}}</div>
      <p>{{content}}</p>
    </a>
  {{/each}}
</section>